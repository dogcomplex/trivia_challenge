export const API_ROOT_URL = 'https://opentdb.com/api.php';
export const QUIZ_LENGTHS = [5, 10, 20, 30, 40, 50];
// the various quiz length options we can cycle through on the Home screen
export const QUIZ_TYPES = ['boolean', 'multiple', null];
// the various quiz types we can cycle through on the home screen
// null = mixed boolean and multiple choice
export const QUIZ_DIFFICULTIES = ['easy', 'medium', 'hard'];
// the various difficulty levels we can cycle through on the home screen
